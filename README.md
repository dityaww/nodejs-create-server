## Membuat server dengan NodeJS

- config project NodeJS dengan perintah `npm init --y`
- import module `http` in your file

### Konfigurasi server

- set port
- set host to localhost
- set handler
- set request and response
- set path routing
